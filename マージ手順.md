# マージ手順

## 1. コミットメッセージの規則
- `BREAKING CHANGE:` - 破壊的な変更
- `feat:` - 新機能の追加
- `fix:` - バグ修正
- その他 - 軽微な更新

## 2. 変更の保存
```bash
# 変更をステージングに追加
git add .

# 規則に従ってコミット
git commit -m "feat: 新機能の追加"
```

## 3. CHANGELOG の確認
- `CHANGELOG.md` が自動生成されます
- 内容を確認し、必要に応じて編集してください

## 4. 現在の変更を保存（コミット）します
```bash
git add .
git commit -m "fix: total_only mode calculation and display"
```

## 5. mainブランチに切り替えて最新化します
```bash
git checkout main
git pull origin main
```

## 6. developブランチをmainにマージします
```bash
git merge develop
```

## 7. コンフリクトが発生した場合は解消してください
## 8. マージ完了後、リモートのmainブランチを更新します
```bash
git push origin main
```

## 9. 開発を続ける場合は、developブランチに戻ります
```bash
git checkout develop
```