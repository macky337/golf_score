# マージ手順

## 0. 環境変数の確認
1. Supabaseのダッシュボードから以下の情報を取得:
   - Project URL → SUPABASE_URL
   - Project API Keys → anon/public → SUPABASE_KEY

2. 環境変数の設定:
```bash
# Windows PowerShellの場合
$env:SUPABASE_URL="your-project-url"
$env:SUPABASE_KEY="your-anon-key"

# Linuxの場合
export SUPABASE_URL="your-project-url"
export SUPABASE_KEY="your-anon-key"
```

## 1. コミットメッセージの規則
- `BREAKING CHANGE:` - 破壊的な変更
- `feat:` - 新機能の追加
- `fix:` - バグ修正
- その他 - 軽微な更新

## 2. 変更の保存
```bash
# 変更をステージングに追加
git add .

# 規則に従ってコミット
git commit -m "feat: 新機能の追加"
```

## 3. CHANGELOG の確認
- `CHANGELOG.md` が自動生成されます
- 内容を確認し、必要に応じて編集してください

## 4. 現在の変更を保存（コミット）します
```bash
git add .
git commit -m "fix: total_only mode calculation and display"
```

## 5. mainブランチに切り替えて最新化します
```bash
git checkout main
git pull origin main
```

## 6. developブランチをmainにマージします
```bash
git merge develop
```

## 7. コンフリクトが発生した場合は解消してください
## 8. マージ完了後、リモートのmainブランチを更新します
```bash
git push origin main
```

## 9. 開発を続ける場合は、developブランチに戻ります
```bash
git checkout develop
```

データ診断機能の使い方
データ診断機能はGolfスコアアプリケーションのデータ整合性を確認・修復するためのツールです。以下の2つの方法で利用できます。

1. デバッグモード経由で使用する場合
アプリのホーム画面（Main.py）で右サイドバーの「開発者オプション」を展開します
「🔧 デバッグモード開始」ボタンをクリックします
サイドバーに「診断ツール」セクションが表示されます
「データ整合性チェック」ボタンをクリックします
チェックが実行され、問題があれば詳細が表示されます
問題が見つかった場合は「問題を修復する」ボタンをクリックすることで修復できます

# 基本的な使用方法（最新の10件のスコアデータを表示）
python check_round_data.py

# 特定のラウンド（例：ラウンドID 39）を詳細に確認
python check_round_data.py -r 39

# すべてのラウンド情報を表示
python check_round_data.py -a

# 表示するレコード数を制限（例：20件）
python check_round_data.py -l 20

# 複数のオプションを組み合わせる
python check_round_data.py -r 39 -a