Golf Score App マニュアル
1. アプリケーションの概要
本アプリケーションは、ゴルフのスコア管理、マッチ戦の記録、ポイント集計を行うためのツールです。
ユーザーはメンバー登録、ラウンド設定、スコア入力などを行い、最終的な結果確認・PDF出力・ポイント集計を実施できます。
なお、各フェーズで入力されたスコアやパット数、ゲームポイントは、内部ロジックに基づいて計算され、その結果が round_results テーブルに保存されます。

2. 基本的な使用手順
2.1. メンバー登録
  1. メインメニューから「メンバー登録」を選択
  2. メンバー名を入力
  3. 「登録」ボタンをクリック
使用テーブル:
  • member
    ○ メンバーIDや名前を管理するテーブル

2.2. ラウンド設定
  1. メインメニューから「ラウンド設定」を選択
  2. 以下の項目を設定：
    ○ プレイ日付
    ○ ゴルフ場名（新規登録または既存コースから選択）
    ○ プレイ人数（3名または4名）
    ○ 参加メンバー
    ○ ハンディキャップの設定（handicap_match テーブルに保存）
    ○ マッチ方式（Total Only など）
  3. 「保存」ボタンをクリックしてラウンド情報を登録
使用テーブル:
  • rounds
    ○ ラウンドの基本情報（プレイ日、コース名、人数など）を管理
  • handicap_match
    ○ 対戦メンバー間のハンディキャップや Total Only モードなどを設定

2.3. スコア入力と round_results への書き込み
ラウンド設定完了後、以下の手順でスコアを入力し、内部ロジックに基づいた計算結果を round_results テーブルへ書き込みます。
2.3.1. フロントスコア入力
  • 入力項目:
    ○ 各ホールのスコア (front_score)
    ○ パット数 (front_putt)
    ○ ゲームポイント (front_game_pt)
  • 処理内容:
    ○ 入力されたフロントスコアをもとに、calc_net_score などのロジックを用いてハンディ補正後のネットスコアや部分的なゲームポイントを算出
    ○ 算出されたマッチポイントやパットポイントの一部を round_results テーブルに書き込み
    ○ バックスコア入力画面へ遷移、または結果確認画面へ遷移して途中経過を確認可能
2.3.2. バックスコア入力
  • 入力項目:
    ○ 各ホールのスコア (back_score)
    ○ パット数 (back_putt)
    ○ ゲームポイント (back_game_pt)
  • 処理内容:
    ○ バックスコア入力後、フロントスコアと合算して calc_net_total などを実行し、トータルスコアおよび各セクションのマッチポイントを再計算
    ○ 再計算結果を round_results テーブルに上書き保存し、最新状態を維持
    ○ エキストラスコア入力画面へ遷移、または結果確認画面で途中経過を確認
2.3.3. エキストラスコア入力（必要な場合のみ）
  • 入力項目:
    ○ 追加ホールのスコア (extra_score)
    ○ パット数 (extra_putt)
    ○ ゲームポイント (extra_game_pt)
  • 処理内容:
    ○ エキストラスコア入力後、calc_net_extra などのロジックを用いて計算し、マッチ戦の結果に影響するポイントを算出
    ○ round_results テーブルに書き込み、最終的な総合得点 (total_pt) の計算に反映
    ○ 結果確認画面へ遷移し、入力時点での途中経過を確認
使用テーブル:
  • score
    ○ スコアやパット数、ゲームポイント、マッチポイントなどの生データを管理
  • round_results
    ○ ロジック計算後のマッチポイント (match_front / match_back / match_total / match_extra / match_pt)、パットポイント (putt_pt)、総合得点 (total_pt) を保存
    ○ match_pt + putt_pt + total_game_pt = total_pt のように集計され、最終結果を管理

2.4. 結果確認・出力
  1. メインメニューから「結果確認」を選択
  2. 入力したスコアおよびロジック計算後のマッチポイントやパットポイントが round_results テーブルから読み込まれ、画面に表示
  3. 「PDF出力」ボタンをクリックすると、スコア表をPDF形式で生成してダウンロード可能
  4. 「ラウンド確定」ボタンを押すと、最終結果が確定され、以降の変更は管理画面で行う

2.5. ポイント集計
  1. メインメニューから「ポイント集計」を選択
  2. 集計期間（通算、年度別、月別）を選択
  3. 各プレイヤーの総合成績を確認可能

2.6. 管理機能
2.6.1. データ修正
  1. メインメニューから「管理画面」を選択
  2. 「スコア修正」「ハンディキャップ修正」「メンバー管理」など、修正対象のタブを選択
  3. 必要なデータを修正

3. ゴルフスコア計算ロジックと結果表示の概要
3.1 ネットスコア計算
  • calc_net_score
    ○ 例として front_score を対象とし、
net_score = front_score - handicap
として生スコアからハンディキャップを差し引いたネットスコアを算出
  • calc_net_total
    ○ Front Score と Back Score を合計し、
net_total = (front_score + back_score) - (handicap × 2)
としてトータルスコアを求める
  • calc_net_extra
    ○ extra_score から
net_extra = extra_score - handicap
を計算し、エキストラスコアのネット値を求める

3.2 パット得点計算
  • calc_putt_points
    ○ 各プレイヤーのパットスコアを集め、最小スコアを出したプレイヤーに応じて得点を付与
    ○ 3人プレイ:
      § 勝者1名 → +20pt、残り2名 → -10pt
      § 勝者2名 → 各勝者 +5pt、残り1名 → -10pt
    ○ 4人プレイ:
      § 勝者1名 → +30pt、残り3名 → -10pt
      § 勝者2名 → 各勝者 +10pt、残り2名 → -10pt
      § 勝者3名 → 各勝者 +10pt、残り1名 → -30pt

3.3 マッチポイント計算
  • calc_match_points_by_section
    ○ Front / Back / Total / Extra 各セクションごとにハンディ補正後のスコア差を比較し、勝者 +10、敗者 -10、同点 0
  • calc_match_points
    ○ 2人プレイヤー間で以下を判定：
      § バックスコア未入力:
        □ Total Only → 判定せず 0pt
        □ 通常モード → フロントのみで勝敗判定
      § バックスコア入力済み:
        □ Total Only → Front + Back の合計、エキストラ含む場合は比較し ±10pt
        □ 通常モード → Front / Back / Total / Extra をそれぞれ±10pt計算し合算

3.4 ゲームポイント (Game Pt) 計算
  • 4人プレイ
    ○ 各プレイヤーの最終 Game Pt は、
front_game_pt + back_game_pt + extra_game_pt
を合計した値
  • 3人プレイ
    ○ 一時的に Game Pt を算出後、
最終 total_game_pt = (自分のtemp_game_pt × 2) - (他2人のtemp_game_pt合計)
    ○ これにより、3人プレイ時の得点バランスを調整

4. 結果表示画面
4.1 スコア詳細表
  • 表示内容
    ○ 各プレイヤーの Front / Back / Extra / Total Score
    ○ ゲームポイント (Game Pt)
    ○ パットポイント (Put Pt)
    ○ マッチポイント (Match Front, Match Back, Match Total, Match Extra, Match Pt)
  • データの出所
    ○ スコアそのものは score テーブルに保存
    ○ ロジック計算後のマッチ・パットポイントは round_results テーブルから取得
    ○ match_pt + putt_pt + total_game_pt = total_pt という形で最終結果が保存される

4.2 マッチ対戦表 (星取表)
  • 作成方法
    ○ create_match_matrix() 関数で各ペアの対戦結果を再計算し、星取表形式で表示
  • 表示上の注意
    ○ Total Only モードでバックスコア未入力の場合は 0または"-"表示
    ○ 内部計算結果とは符号を反転し、UI上で「+10」「-10」をわかりやすく表現

4.3 詳細なマッチ結果表
  • 作成方法
    ○ create_detailed_match_results() 関数で各対戦カードごとのマッチポイントを算出
    ○ マルチインデックス形式で「対戦カード名」「使用ハンディキャップ」などを表示
  • 注意点
    ○ Total Only モードでバックスコア未入力の場合、判定は行わず "-" や 0 表示

5. テーブルおよびカラム一覧
5.1 rounds テーブル
ゴルフのラウンドごとに、日付・コース情報・人数などの基本データを保持します。
カラム	説明
round_id	ラウンドの一意ID。主キー
date	予備の日付。旧バージョン互換などに使用
date_played	実際にプレーした日付
course_name	コース名。既存コース or 新規登録
num_players	参加プレイヤー人数（3 or 4）
has_extra	エキストラホールの有無（True/False）
finalized	ラウンドが確定済みかどうか（True/False）
created_at	レコード作成日時
course_id	courses テーブルの ID（外部キー）

5.2 score テーブル
各ラウンド・各メンバーのスコアやパット数、ゲームポイントを管理します。
ラウンド入力画面（フロント/バック/エキストラ）で入力された生スコアを保存します。
カラム	説明
score_id	スコアの一意ID。主キー
round_id	ラウンドID（roundsテーブルの外部キー）
member_id	メンバーID（memberテーブルの外部キー）
front_score	フロント9のスコア
back_score	バック9のスコア
extra_score	エキストラホールのスコア（必要な場合）
front_putt	フロント9のパット数
back_putt	バック9のパット数
extra_putt	エキストラホールのパット数
front_game_pt	フロント9のゲームポイント
back_game_pt	バック9のゲームポイント
extra_game_pt	エキストラホールのゲームポイント
total_pt	（従来互換）総合ポイント。計算ロジック適用後は round_results を参照
created_at	レコード作成日時
temp_game_pt	一時的なゲームポイント（3人プレイなど）
total_game_pt	最終的なゲームポイント（フロント+バック+エキストラ）
total_score	フロント+バック+エキストラの生スコア合計（ハンディ未調整）


5.4 handicap_match テーブル
ラウンド内で対戦するメンバー同士のハンディキャップ情報や、Total Only モードの設定などを管理します。
カラム	説明
id	ハンディキャップ設定の一意ID。主キー
round_id	ラウンドID（roundsテーブルの外部キー）
player_1_id	対戦プレイヤー1のID（memberテーブル）
player_2_id	対戦プレイヤー2のID（memberテーブル）
player_1_to_2	プレイヤー1からプレイヤー2へ与えるハンディ値
player_2_to_1	プレイヤー2からプレイヤー1へ与えるハンディ値
total_only	Trueの場合、トータルスコアのみでマッチ判定
created_at	レコード作成日時

5.5 round_results テーブル
ロジック計算後のマッチポイントやパットポイント、最終得点などを集約するテーブルです。
スコア入力後に計算を行い、ここへ保存することで最終結果を管理します。
カラム	説明
id	ラウンド結果の一意ID。主キー
round_id	ラウンドID（roundsテーブルの外部キー）
member_id	メンバーID（memberテーブルの外部キー）
match_front	フロント9のマッチポイント
match_back	バック9のマッチポイント
match_total	トータルのマッチポイント
match_extra	エキストラホールのマッチポイント
match_pt	総合マッチポイント（例: front+back+extra）
putt_pt	パットポイント
temp_game_pt	一時的なゲームポイント（3人プレイ時など）
total_game_pt	最終的なゲームポイント
created_at	レコード作成日時
updated_at	レコード更新日時
total_pt	総合得点（match_pt + putt_pt + total_game_pt など）

5.6 courses テーブル
ゴルフ場のコース情報を管理するテーブルです。
カラム	説明
id	コースの一意ID
name	コース名
created_at	レコード作成日時

5.7 member テーブル
メンバーを管理するテーブルです。
members テーブルがエラーのため、現在はこちらの「member」テーブルを使用している可能性があります。
カラム	説明
member_id	メンバーの一意ID
name	メンバー名
created_at	レコード作成日時

補足
   • スコア入力時には、まず score テーブルに生データが保存され、計算後に round_results テーブルへマッチポイントやパットポイント、最終得点が反映されます。
  • handicap_match テーブルを参照して各プレイヤー間のハンディキャップを計算し、マッチ戦の勝敗を判定します。
  • courses テーブルはコース情報を登録・管理するためのテーブルです。
これらのテーブルを連携させることで、ゴルフスコアの入力から最終結果の集計・表示までをスムーズに管理できます。
