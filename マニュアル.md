# Golf Score App マニュアル

## 1. アプリケーションの概要
このアプリケーションは、ゴルフのスコア管理、マッチ戦の記録、ポイント集計を行うためのツールです。

## 2. 基本的な使用手順

### 2.1. メンバー登録
1. メインメニューから「メンバー登録」を選択
2. メンバー名を入力
3. 「登録」ボタンをクリック

### 2.2. ラウンド設定
1. メインメニューから「ラウンド設定」を選択
2. 以下の項目を設定：
   - プレイ日付
   - ゴルフ場名（新規または過去のコースから選択）
   - プレイ人数（3名または4名）
   - 参加メンバー
   - ハンディキャップの設定
   - マッチ方式

### 2.3. スコア入力
1. フロントスコア入力
   - 各ホールのスコアを入力　front_score
   - パット数を入力　 front_putt
   - ゲームポイント　front_game_pt
2. バックスコア入力
   - 各ホールのスコアを入力　back_score
   - パット数を入力　back_putt
   - ゲームポイント　back_game_pt
3. エキストラスコア入力（必要な場合）
   - 追加ホールのスコアを入力　extra_score
   - パット数を入力　extra_putt
   - ゲームポイント　extra_game_pt

### 2.4. 結果確認・出力
1. メインメニューから「結果確認」を選択
2. 入力したスコアとマッチ結果を確認
3. PDFボタンをクリックしてレポートを出力

### 2.5. ポイント集計
1. メインメニューから「ポイント集計」を選択
2. 集計期間を選択：
   - 通算成績
   - 年度別
   - 月別

## 3. 管理機能
### 3.1. データ修正
1. メインメニューから「管理画面」を選択
2. 修正したいデータのタブを選択：
   - スコア修正
   - ハンディキャップ修正
   - メンバー管理

## 4. 注意事項
- スコア入力は必ず「ラウンド設定→フロント→バック→エキストラ」の順で行ってください
- データ修正が必要な場合は、管理画面から修正してください
- 入力したデータは必ず結果確認画面で確認してください


=== scoreテーブルの構造 ===
カラム一覧:
- score_id
- round_id
- member_id
- front_score
- back_score
- extra_score
- front_putt
- back_putt
- extra_putt
- front_game_pt
- back_game_pt
- extra_game_pt
- match_front
- match_back
- match_total
- match_extra
- match_pt
- putt_pt
- total_pt
- created_at
- temp_game_pt
- total_game_pt




# ゴルフスコア計算ロジックと結果表示の概要

以下は、計算ロジックと結果確認画面で各表にどのように数値が表示されるかの概要です。

---

## 1. 計算ロジック

### 1.1 ネットスコア計算
- **calc_net_score**  
  指定されたセクション（例："Front Score"）について、  
  `score - (handicap * multiplier)` によりネットスコアを算出します。

- **calc_net_total**  
  "Front Score" と "Back Score" の合計からハンディキャップ×2を引いた値を計算し、トータルスコアを得ます。

- **calc_net_extra**  
  "Extra Score" に対しハンディキャップ×multiplierを引いて、エキストラスコアのネット値を求めます。

### 1.2 パット得点計算
- **calc_putt_points**  
  パット戦の得点は、参加人数に応じた以下のルールで計算されます:
  1. 各プレイヤーのパットスコア（例："Putt Front", "Putt Back", "Putt Extra" など）を収集し、参加者全体のスコアリストから最小値（min_score）を特定します。  
  2. 最小スコアを出したプレイヤー（winners）のリストを作成します。  
  3. 【3人の場合】  
     - 最小スコアを出したプレイヤーが1名の場合:  
       - 勝者に +20pt  
       - 残りの2名に -10pt を割り当てる。  
     - 最小スコアを出したプレイヤーが2名の場合:  
       - 両勝者に +5pt  
       - 残り1名に -10pt を割り当てる。  
  4. 【4人の場合】  
     - 最小スコアを出したプレイヤーが1名の場合:  
       - 勝者に +30pt  
       - 残りの3名に -10pt を割り当てる。  
     - 最小スコアを出したプレイヤーが2名の場合:  
       - 両勝者に +10pt  
       - 残りの2名に -10pt を割り当てる。  
     - 最小スコアを出したプレイヤーが3名の場合:  
       - その3名に各々 +10pt  
       - 残り1名に -30pt を割り当てる。

### 1.3 マッチポイント計算
- **calc_match_points_by_section**  
  指定セクション（"Front", "Back", "Total", "Extra"）ごとに、  
  ハンディキャップ補正後のスコアの差から±10ptを返します。  
  ※プレイヤー i の方が良い場合は +10、悪い場合は -10、同点の場合は0。

- **calc_match_points**  
  2人のプレイヤー間で以下の場合の計算を行います：
  - **バック９未入力の場合**（＝フロント入力のみ）：  
    - **Total Onlyモード**  
      → 判定すべきではなく、0を返します。  
    - **通常モード**  
      → フロントとエキストラのみで判定し、Match Frontの値をそのままMatch Totalとし、合計は `front_pt + extra_pt` となります。
  - **バック９入力がある場合**：  
    - **Total Onlyモード**  
      → トータルスコア (Front＋Back) および Extra Score を比較し、Total Onlyとして ±10pt（各セクション別にExtraを加味）を算出します。
    - **通常モード**  
      → Front, Back, Total, Extraそれぞれの比較結果 (各+10または-10) を合算し、内部では `{ "Match Front": ..., "Match Back": ..., "Match Total": ..., "Match Extra": ..., "Total": ... }` の辞書形式で返されます。

### 1.4 ゲームポイント（Game Pt）計算
- **計算方法**：
  プレーヤー人数に応じて2つの異なる計算方式を採用します：

  1. **4人プレイの場合**：
     - 各プレイヤーの最終的なGame Ptは以下の合計となります：
       ```
       Game Pt = Front GP + Back GP + Extra GP
       ```

  2. **3人プレイの場合**：
     - 各プレイヤーの最終的なGame Ptは以下の計算式で算出されます：
       ```
       一時的なGame Pt = Front GP + Back GP + Extra GP
       最終Game Pt = (一時的なGame Pt × 2) - (他の2人の一時的なGame Ptの合計)
       ```
     - この計算方式により、3人プレイ時の得点バランスを調整します。

- **計算の流れ**：
  1. 各プレイヤーの Front GP、Back GP、Extra GP を合算し、一時的なGame Ptを算出
  2. プレイヤー数に応じて最終的なGame Ptを計算：
     - 4人の場合：一時的なGame Ptをそのまま採用
     - 3人の場合：上記の計算式を適用して再計算

---

## 2. 結果表示の画面

### 2.1 スコア詳細表
- **内容**：  
  各プレイヤーの基本スコア (Front, Back, Extra, Total Score) と、  
  ゲームポイント（Game Pt）、パットポイント（Put Pt）、およびマッチポイント（Match Front, Match Back, Match Total, Match Extra, Match Pt）を表示します。
- **ポイント算出**：  
  `calculate_player_points()` 関数により、各プレイヤーの総合得点（Game Pt + Match Pt + Put Pt）が算出され、各セクションのマッチポイントはUI用に大文字のフィールド名 ("Match Front" 等) で格納されます。

### 2.2 マッチ対戦表 (星取表)
- **作成方法**：  
  `create_match_matrix()` 関数が、各ペアの対戦結果を生データから再計算して生成します。  
  - 生データは、バック９が入力されていない場合は判定を行わず、Total Onlyペアは0（または "-" 表示）とします。  
  - 計算結果の符号は、内部計算と逆に出力することで、表示側で「プラス＝優勢」となるように調整されています（例：内部で荒巻が+10なら、対戦表では-10として逆転表示し、見た目の対称性を確保）。
- **表示上の注意**：  
  対戦表の出力では、例として  
  - 「荒巻 vs 吉井」（Total Onlyモードの場合）は、バック入力がないため "-" または 0 表示が期待されます。  
  - 「荒巻 (45) vs 福澤 (47)」では、荒巻が+10となるべきで、その逆も同様に反転して表示されます。

### 2.3 詳細なマッチ結果表
- **作成方法**：  
  `create_detailed_match_results()` 関数により、各対戦カードごとに、  
  各プレイヤーのマッチポイント（累計された「Total」）が表示され、マルチインデックスにより対戦カード名および使用ハンディキャップが表示されます。
- **注意点**：  
  Total Onlyの対戦では、バック９未入力の場合、判定が行われず "-" または 0 表示になります。

---

## 3. 表示ロジックとの整合性
- **フィールド名の整合性**：  
  - 計算ロジック側では、内部で "match_pt" など小文字のフィールドで保持していますが、UIに渡す際は "Match Front", "Match Total", "Match Pt" など大文字のキーと一致させて表示しています。
- **符号の調整**：  
  - マッチ対戦表では、各対戦の計算結果辞書から "Total" の値を取得し、その符号を反転させることで、表示側の優劣の判定が正しくなるようにしています。
- **バック９未入力時の特例処理**：  
  - バックスコアがない場合は、Total Onlyモードでは判定を行わず、通常モードではフロント＋エキストラのみで計算する仕様となっています。  
  これらの仕様により、実際の入力状況に応じた柔軟な表示が実現されています。

---

以上が、計算ロジックおよび結果確認画面における各表の表示ロジック概要です。
